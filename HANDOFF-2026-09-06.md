# Handoff — 2026-09-06 — Marketing screenshot walkthrough

## What was asked

Walk every Distavo feature, capture labelled screenshots for the landing page, and collect
console/page errors into a triage list as Vikunja tasks (`marketing-screenshot-walkthrough` +
`riera-working-conventions` skills).

## What changed

- **`docs/screenshots/2026-09-06-walkthrough/`** (committed, `d0b4153`): 8 labelled PNGs + a
  `walkthrough-report.md` explaining method, what was and wasn't captured (and why), and the
  findings below. These are **not yet promoted** to `apple/metadata/screenshots/` (the set the
  Setapp listing currently uses) — that's Marc's call once he's reviewed them.
- **`~/.claude/skills/marketing-screenshot-walkthrough/SKILL.md`** (claude-config repo, committed
  `2d96108`): added a "Native macOS app" subsection — the osascript/System Events pattern, the
  `$HOME`-override-doesn't-work gotcha, and the privacy lessons from two near-misses this session.
- **Memory**: `distavo-shared-config-no-isolation.md` — Distavo's shared config/data path across
  every build/edition, and why that means never launching a local build alongside the live app.
- **Vikunja**: created project 58 ("Distavo", nested under empresa/projecte) since none existed;
  filed 3 real findings (below) plus the tracking task (marked done).

## What's verified

- Walked and screenshotted: main menu, Activity submenu (real log), Help submenu, all 3 Settings
  form sections (General, Transcription/Summarisation, Connections/Updates), notes folder,
  recordings folder — against the **live, already-running production app**, entirely read-only
  (confirmed with Marc before starting: no Save/Process now/Record meeting/local-Ollama-toggle/
  Watch interval/Pause/Quit clicks).
- Confirmed via `log show --predicate 'process == "Distavo"' --last 30m`: the walkthrough itself
  produced zero errors/crashes in the live process.
- `apple/DistavoEmbedded/Package.resolved` pollution from my verification build was reverted
  before committing (known gotcha, `distavo-native-verify` skill).
- A Debug build (`apple/build-screenshot/`, gitignored) confirmed current `main` still compiles
  for the Direct edition — built but never launched (see shared-config risk above).

## Findings filed to Vikunja (project 58, all priority 2)

1. **#1827** — two 2026-07-07 recordings (`12.36.59.wav`, `14.47.35.wav`, both 4 KB) are
   permanently stuck `.failed` with no note, silently, since 2026-07-31.
2. **#1828** — the offline-model-download error message leads with "model not found, check the
   repo name" when the real cause is "no internet connection" — misleading troubleshooting path.
3. **#1829** — `WatcherController.hasLastNote` never seeds from disk on launch, so "Copy last
   transcript"/"Open last note" stay disabled after every restart even with real notes present.

## What's left / not done

- **Not pushed to `origin`.** Both commits (`distavo` `d0b4153`, `claude-config` `2d96108`) are
  local on `main` — push whenever you're ready; nothing here needs urgency.
- **Screenshot promotion decision**: do the new screenshots replace `apple/metadata/screenshots/`
  (currently referenced as "Ready" in `docs/setapp/ASSETS_CHECKLIST.md`), or sit alongside as a
  refreshed set to choose from? Left for you — see the walkthrough report for what's in each.
- **Not captured** (see report for full reasoning): the Permissions sheet, a live "Test
  connection" result, the "Record meeting" active state, and any real note's actual content
  (deliberately, to avoid exposing private transcript text in a public screenshot).
- The three Vikunja findings are UI-health observations, not fixed — none are urgent/blocking.

## Note on the working environment

Several other Claude sessions were actively running against this Mac and this same repo/global
config during this session (visible via `ps aux`, and `CLAUDE.md`/global `CLAUDE.md` changing on
disk mid-task from another session's commits). No conflicts arose — all edits here were scoped to
new files or a single targeted revert — but if screenshots or Vikunja tasks look duplicated, that's
worth a quick sanity check.
